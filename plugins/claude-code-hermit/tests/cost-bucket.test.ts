import { it, expect } from 'bun:test';
import { taskFixture, taskLib } from './helpers/tasks';
import { SOURCE_ATTRIBUTION_VERSION } from '../scripts/lib/cost-log';

for (const source of ['heartbeat', 'routine:daily', 'other', 'channel:discord', 'peer']) it(`attributes ${source} without a binding`, async () => { const f = taskFixture(); try { const lib = await taskLib(); expect(lib.resolveTaskAttribution(f.dir, 'resident', source, null)).toMatchObject({ task_id: null, bucket: source === 'heartbeat' || source.startsWith('routine:') ? 'duties' : 'conversation', attribution: 'source' }); } finally { f.cleanup(); } });
it('two admissions retain the binding and first-bound task wins reversed unequal work', async () => { const f = taskFixture(); try { const lib = await taskLib(); f.put('state/execution.json', { cc_session_id: 'resident', state: 'in_flight', at: new Date().toISOString(), turn_id: 'first' }); const a = await f.open(); const b = await f.open(); f.put('state/execution.json', { cc_session_id: 'resident', state: 'in_flight', at: new Date().toISOString(), turn_id: 'second' }); await f.ok('note', [b.id], 'Most work'); expect(lib.resolveTaskAttribution(f.dir, 'resident', 'other', null)).toMatchObject({ task_id: a.id, task_ids: [a.id, b.id], attribution: 'binding', bucket: 'tasks' }); } finally { f.cleanup(); } });
it('interrupted old work does not attribute after the last logged row', async () => { const f = taskFixture(); try { const lib = await taskLib(); f.put('state/task-turn.json', { cc_session_id: 'resident', task_ids: ['T-20260914-120000'], at: new Date(Date.now() - 1000).toISOString() }); expect(lib.resolveTaskAttribution(f.dir, 'resident', 'other', new Date().toISOString()).task_id).toBeNull(); } finally { f.cleanup(); } });
it('helper ownership follows current conversation session and most recently closed task', async () => { const f = taskFixture(); try { const lib = await taskLib(); f.put('state/conversations.json', { 'discord:c1': { session_id: 'helper' } }); const { id } = await f.open(['--owner', 'helper:discord:c1', '--conversation', 'discord:c1']); expect(lib.resolveTaskAttribution(f.dir, 'helper', 'other', null)).toMatchObject({ task_id: id, attribution: 'helper-conversation' }); await f.ok('cancel', [id, '--actor', 'discord:u1', '--reason-stdin'], 'Cancelled'); expect(lib.resolveTaskAttribution(f.dir, 'helper', 'other', null).task_id).toBe(id); f.put('state/conversations.json', { 'discord:c1': { session_id: 'replacement' } }); expect(lib.resolveTaskAttribution(f.dir, 'replacement', 'other', null).task_id).toBe(id); expect(lib.resolveTaskAttribution(f.dir, 'helper', 'other', null).task_id).toBeNull(); } finally { f.cleanup(); } });
it('async dispatch uses the first main row at or after its boundary', async () => { const lib = await taskLib(); const rows = [{ cc_session_id: 'resident', observed_at: '2026-09-14T10:00:00Z', task_id: 'T-20260914-100000', bucket: 'tasks' }, { cc_session_id: 'resident', observed_at: '2026-09-14T11:00:00Z', task_id: 'T-20260914-110000', bucket: 'tasks' }]; const f = taskFixture(); try { expect(lib.dispatchTaskAttribution(f.dir, rows, 'resident', '2026-09-14T10:30:00Z').task_id).toBe(rows[1].task_id); } finally { f.cleanup(); } });
it('pre-upgrade rows default to conversation and buckets partition the total', async () => { const lib = await taskLib(); const rows = [{ cost_usd: 1 }, { cost_usd: 2, bucket: 'tasks' }, { cost_usd: 3, bucket: 'duties' }]; expect(lib.costBucket(rows[0])).toBe('conversation'); expect(['tasks', 'conversation', 'duties'].map(b => rows.filter(r => lib.costBucket(r) === b).reduce((n, r) => n + r.cost_usd, 0)).reduce((a, b) => a + b, 0)).toBe(6); expect(SOURCE_ATTRIBUTION_VERSION).toBe(2); });
