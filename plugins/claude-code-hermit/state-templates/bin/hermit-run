#!/usr/bin/env bash
# Dispatcher for hermit boot scripts — resolves plugin path by scanning installed
# plugins, then hands off to the plugin-shipped scripts/hermit-exec.sh, which owns
# the script-name → (file, runtime) mapping. Keeping that mapping plugin-side means
# this wrapper survives runtime changes without another on-disk refresh.
# Usage: hermit-run <script-name> [args...]
# Called by hermit-start, hermit-stop, and hermit-watchdog wrappers.
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
CONFIG="$SCRIPT_DIR/../config.json"
TARGET="${1:?Usage: hermit-run <script-name> [args...]}"
shift

if [ ! -f "$CONFIG" ]; then
  echo "[hermit] No config found at $CONFIG" >&2
  echo "[hermit] Run /claude-code-hermit:hatch inside Claude Code first." >&2
  exit 1
fi

# --- Resolve plugin root ---
# Priority: HERMIT_PLUGIN_ROOT env var > scan ~/.claude/plugins/
# No config.json dependency — works in both host and Docker without path conflicts.

PLUGIN_ROOT=""

# Confirm a directory carries the claude-code-hermit plugin manifest.
_is_core_root() {
  [ -f "$1/.claude-plugin/plugin.json" ] || return 1
  local n
  n=$(grep -o '"name"[^,]*' "$1/.claude-plugin/plugin.json" 2>/dev/null | head -1 | cut -d'"' -f4)
  [ "$n" = "claude-code-hermit" ]
}

# 1. Env var (set by docker-entrypoint) — accepted only if it actually points at
# core's plugin root. A stale/foreign value is ignored (not fatal), so a bad hint
# never boots the wrong tree and never blocks the scan below.
if [ -n "${HERMIT_PLUGIN_ROOT:-}" ] && [ -d "$HERMIT_PLUGIN_ROOT" ]; then
  if _is_core_root "$HERMIT_PLUGIN_ROOT"; then
    PLUGIN_ROOT="$HERMIT_PLUGIN_ROOT"
  else
    echo "[hermit] HERMIT_PLUGIN_ROOT does not point at claude-code-hermit; ignoring: $HERMIT_PLUGIN_ROOT" >&2
  fi
fi

# 2. Scan marketplace cache for the claude-code-hermit plugin under
# plugins/<name>/ (monorepo layout — gtapps/claude-code-hermit ships as a
# multi-plugin marketplace). Collect ALL matches and fail loud on ambiguity
# (two marketplaces shipping core) rather than silently taking glob order.
if [ -z "$PLUGIN_ROOT" ]; then
  SEARCH_BASE="${CLAUDE_CONFIG_DIR:-$HOME/.claude}"
  _old_nullglob=$(shopt -p nullglob 2>/dev/null || true)
  shopt -s nullglob
  _matches=()
  for d in "$SEARCH_BASE"/plugins/marketplaces/*/plugins/*/; do
    if [ -d "$d/scripts" ] && _is_core_root "${d%/}"; then
      _matches+=("${d%/}")
    fi
  done
  $_old_nullglob
  # Each glob match is a distinct marketplace/plugin path, so _matches never holds
  # duplicates — count the array directly instead of a sort -u no-op on the boot path.
  if [ "${#_matches[@]}" -gt 0 ]; then
    if [ "${#_matches[@]}" -gt 1 ]; then
      echo "[hermit] Ambiguous claude-code-hermit install — multiple marketplaces provide it:" >&2
      printf '[hermit]   %s\n' "${_matches[@]}" >&2
      echo "[hermit] Set HERMIT_PLUGIN_ROOT to the one to use." >&2
      exit 1
    fi
    PLUGIN_ROOT="${_matches[0]}"
  fi
fi

if [ -z "$PLUGIN_ROOT" ] || [ ! -d "$PLUGIN_ROOT" ]; then
  echo "[hermit] Plugin root not found or invalid: $PLUGIN_ROOT" >&2
  echo "[hermit] Run /claude-code-hermit:hermit-evolve inside Claude Code to fix." >&2
  exit 1
fi

DISPATCH="$PLUGIN_ROOT/scripts/hermit-exec.sh"
if [ ! -f "$DISPATCH" ]; then
  echo "[hermit] hermit-exec.sh not found at $PLUGIN_ROOT/scripts/" >&2
  echo "[hermit] Plugin may be corrupted. Reinstall with: claude plugin install claude-code-hermit@claude-code-hermit --scope local" >&2
  exit 1
fi

exec bash "$DISPATCH" "$TARGET" "$@"
